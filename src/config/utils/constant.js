const L_DEVICES = 1365; // ipad pro potrait atau ipad landscape
const M_DEVICES = 1021; // ipad pro potrait atau ipad landscape
const S_DEVICES = 769; // ipad potrait
const XS_DEVICES = 480; // mobile
const TAB_PX = 1024;

export const L_DEVICES_WIDTH = `${L_DEVICES}px`;
export const M_DEVICES_WIDTH = `${M_DEVICES}px`;
export const S_DEVICES_WIDTH = `${S_DEVICES}px`;
export const XS_DEVICES_WIDTH = `${XS_DEVICES}px`;

export const IS_TAB = window.innerWidth <= TAB_PX;